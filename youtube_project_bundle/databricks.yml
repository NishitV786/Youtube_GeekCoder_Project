# This is a Databricks asset bundle definition for youtube_project_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: youtube_project_bundle
  uuid: 13c7b533-459b-46e4-8d13-1040c055f3c3

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use
  warehouse_id:
    description: WarehouseID

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-283cc898-4cbb.cloud.databricks.com
    variables:
      catalog: youtube_dev
      schema: dev
      warehouse_id: 4dda5f7d80f85fec
    presets:
      artifacts_dynamic_version: true
  prod:
    mode: production
    workspace:
      host: https://dbc-283cc898-4cbb.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/azumaproton@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/azumaproton@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: youtube_prod
      schema: prod
      warehouse_id: 4dda5f7d80f85fec
    permissions:
      - user_name: azumaproton@gmail.com
        level: CAN_MANAGE
